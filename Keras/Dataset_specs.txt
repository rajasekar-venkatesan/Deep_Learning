Dataset I used is too huge to upload. Dataset folder heirarchy is clear from the code. Can use any dataset with similar heirarchy. The images in the dataset needs not essentially be of any particular size.
